# Pinned for reproducible builds. Bump deliberately, not incidentally.
# Verified against pip-audit; CI re-checks on every push.

# --- Web layer ---
fastapi==0.141.1
uvicorn[standard]==0.52.1

# --- Persistence ---
sqlalchemy==2.0.51
psycopg2-binary==2.9.12
alembic==1.18.5

# --- Async pipeline ---
celery[redis]==5.6.3
redis==6.4.0

# --- Model provider & config ---
openai==2.53.0
pydantic==2.13.4
python-dotenv==1.2.2

# --- Test suite ---
# Kept in the runtime image on purpose: the documented way to run the suite is
# `docker compose run --rm api pytest`, against the same image the app runs in.
pytest==9.1.1
httpx==0.28.1
